[package]
name = "yield-market"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk.workspace = true
yield-math.workspace = true

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
